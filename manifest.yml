---
applications:
  - name: cobalt-studio
    memory: 256M
    instances: 1
    buildpacks:
      - nodejs_buildpack
    command: node server.js
    services:
      - cobalt-db
      - cobalt-ai
    env:
      NODE_ENV: production
